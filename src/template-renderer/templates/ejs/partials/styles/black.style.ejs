<style>
    a {
        text-decoration: none;
        text-wrap: nowrap;
    }

    body {
        background-color: rgb(255, 255, 255);
        color: rgb(0, 0, 0);
        font-family: Verdana, Geneva, sans-serif;
        line-height: 24px;
        margin: 0;
        padding: 0;
    }

    h1,
    h2,
    h3,
    h4,
    h5,
    h6,
    p {
        padding-bottom: 16px;
    }

    hr {
        border-top: 1px solid rgb(230, 233, 235);
        margin: 16px 0;
    }

    ul {
        list-style: none;
        margin: 0;
        padding: 0 0 16px 0;
    }

    ul li {
        margin-bottom: 4px;
    }

    /* Utils */

    .box-light {
        background-color: rgb(255, 255, 255);
        /* Using dark gray as the accent (light black) */
        border: 1px solid rgb(51, 51, 51);
        border-radius: 4px;
        color: rgb(0, 0, 0);
        font-size: 16px;
        padding: 8px;
    }

    .box-primary {
        /* Dark gray background for primary accents */
        background-color: rgb(51, 51, 51);
        /* White border to provide contrast */
        border: 1px solid rgb(255, 255, 255);
        border-radius: 4px;
        color: rgb(255, 255, 255);
        font-size: 32px;
        padding: 12px;
        max-width: 480px;
        margin: 0 auto;
    }

    .mb-16 {
        margin-bottom: 16px;
    }

    .text-center {
        text-align: center;
    }

    .text-muted {
        color: rgb(136, 136, 136);
    }

    /* Primary text color updated to dark gray */
    .text-primary {
        color: rgb(51, 51, 51);
    }

    .text-right {
        text-align: right;
    }

    /* Layout */

    .t-body {
        /* Fallback light gray background */
        background-color: rgb(230, 230, 230);
        /* Gradient from dark gray to light gray */
        background: rgb(51, 51, 51);
        background: linear-gradient(180deg,
                rgba(51, 51, 51, 1) 40%,
                rgba(230, 230, 230, 1) 40%,
                rgba(230, 230, 230, 1) 60%);
        padding: 16px;
    }

    .t-header {
        padding: 8px;
        width: 100%;
    }

    .t-main {
        background-color: rgb(255, 255, 255);
        padding: 8px 16px;
        width: 100%;
    }
</style>